-- Canonical Snowplow atomic.events schema (131 columns) in TSV column order.
-- This file is the source of truth for column names and target types.
-- The actual external-table DDL used at runtime declares everything as VARCHAR
-- (see tsv_to_parquet.sql.tmpl), then casts via COPY's SELECT to produce
-- correctly typed parquet output. Reading TSV as VARCHAR avoids DataFusion CSV
-- parse errors on the empty-string-as-NULL convention Snowplow uses.

CREATE TABLE atomic_events (
  app_id                   VARCHAR,
  platform                 VARCHAR,
  etl_tstamp               TIMESTAMP,
  collector_tstamp         TIMESTAMP,
  dvce_created_tstamp      TIMESTAMP,
  event                    VARCHAR,
  event_id                 VARCHAR,
  txn_id                   INTEGER,
  name_tracker             VARCHAR,
  v_tracker                VARCHAR,
  v_collector              VARCHAR,
  v_etl                    VARCHAR,
  user_id                  VARCHAR,
  user_ipaddress           VARCHAR,
  user_fingerprint         VARCHAR,
  domain_userid            VARCHAR,
  domain_sessionidx        INTEGER,
  network_userid           VARCHAR,
  geo_country              VARCHAR,
  geo_region               VARCHAR,
  geo_city                 VARCHAR,
  geo_zipcode              VARCHAR,
  geo_latitude             DOUBLE,
  geo_longitude            DOUBLE,
  geo_region_name          VARCHAR,
  ip_isp                   VARCHAR,
  ip_organization          VARCHAR,
  ip_domain                VARCHAR,
  ip_netspeed              VARCHAR,
  page_url                 VARCHAR,
  page_title               VARCHAR,
  page_referrer            VARCHAR,
  page_urlscheme           VARCHAR,
  page_urlhost             VARCHAR,
  page_urlport             INTEGER,
  page_urlpath             VARCHAR,
  page_urlquery            VARCHAR,
  page_urlfragment         VARCHAR,
  refr_urlscheme           VARCHAR,
  refr_urlhost             VARCHAR,
  refr_urlport             INTEGER,
  refr_urlpath             VARCHAR,
  refr_urlquery            VARCHAR,
  refr_urlfragment         VARCHAR,
  refr_medium              VARCHAR,
  refr_source              VARCHAR,
  refr_term                VARCHAR,
  mkt_medium               VARCHAR,
  mkt_source               VARCHAR,
  mkt_term                 VARCHAR,
  mkt_content              VARCHAR,
  mkt_campaign             VARCHAR,
  contexts                 VARCHAR,
  se_category              VARCHAR,
  se_action                VARCHAR,
  se_label                 VARCHAR,
  se_property              VARCHAR,
  se_value                 DOUBLE,
  unstruct_event           VARCHAR,
  tr_orderid               VARCHAR,
  tr_affiliation           VARCHAR,
  tr_total                 DOUBLE,
  tr_tax                   DOUBLE,
  tr_shipping              DOUBLE,
  tr_city                  VARCHAR,
  tr_state                 VARCHAR,
  tr_country               VARCHAR,
  ti_orderid               VARCHAR,
  ti_sku                   VARCHAR,
  ti_name                  VARCHAR,
  ti_category              VARCHAR,
  ti_price                 DOUBLE,
  ti_quantity              INTEGER,
  pp_xoffset_min           INTEGER,
  pp_xoffset_max           INTEGER,
  pp_yoffset_min           INTEGER,
  pp_yoffset_max           INTEGER,
  useragent                VARCHAR,
  br_name                  VARCHAR,
  br_family                VARCHAR,
  br_version               VARCHAR,
  br_type                  VARCHAR,
  br_renderengine          VARCHAR,
  br_lang                  VARCHAR,
  br_features_pdf          BOOLEAN,
  br_features_flash        BOOLEAN,
  br_features_java         BOOLEAN,
  br_features_director     BOOLEAN,
  br_features_quicktime    BOOLEAN,
  br_features_realplayer   BOOLEAN,
  br_features_windowsmedia BOOLEAN,
  br_features_gears        BOOLEAN,
  br_features_silverlight  BOOLEAN,
  br_cookies               BOOLEAN,
  br_colordepth            VARCHAR,
  br_viewwidth             INTEGER,
  br_viewheight            INTEGER,
  os_name                  VARCHAR,
  os_family                VARCHAR,
  os_manufacturer          VARCHAR,
  os_timezone              VARCHAR,
  dvce_type                VARCHAR,
  dvce_ismobile            BOOLEAN,
  dvce_screenwidth         INTEGER,
  dvce_screenheight        INTEGER,
  doc_charset              VARCHAR,
  doc_width                INTEGER,
  doc_height               INTEGER,
  tr_currency              VARCHAR,
  tr_total_base            DOUBLE,
  tr_tax_base              DOUBLE,
  tr_shipping_base         DOUBLE,
  ti_currency              VARCHAR,
  ti_price_base            DOUBLE,
  base_currency            VARCHAR,
  geo_timezone             VARCHAR,
  mkt_clickid              VARCHAR,
  mkt_network              VARCHAR,
  etl_tags                 VARCHAR,
  dvce_sent_tstamp         TIMESTAMP,
  refr_domain_userid       VARCHAR,
  refr_dvce_tstamp         TIMESTAMP,
  derived_contexts         VARCHAR,
  domain_sessionid         VARCHAR,
  derived_tstamp           TIMESTAMP,
  event_vendor             VARCHAR,
  event_name               VARCHAR,
  event_format             VARCHAR,
  event_version            VARCHAR,
  event_fingerprint        VARCHAR,
  true_tstamp              TIMESTAMP
);
